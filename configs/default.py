###########################
# Default paths (override on the command line if your layout differs)
###########################
# Root of the benchmark datasets (see datasets/DATASET.md for the layout)
DATA_DIR = "./data"
# Cache of pre-extracted backbone features
FEATURE_DIR = "./output/features"
# Root of all run artefacts: results.json, config.json, scores.pth,
# checkpoint.pth and alignment.json
RESULT_DIR = "./output/runs"
